@model PetBucket4.Models.Pet


@{
    ViewBag.Title = "Select_Pet";
    Layout = "~/Views/Shared/_Layout.cshtml"; <!--Defines page layout-->
}

<!--New row-->
<div class="row">
    <!--Main insert-->
    <div class="col-lg-7 large-insert">
        <h1>Pets</h1>
        <p>Choose which pets you would like to stay with us this booking from a list of your pets
        who have previously stayed with us!
        </p>
        <!--foreach example. Displays each pet's name registered to logged in user-->
        

        <!--Begin Form-->
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <h2>Select a Pet</h2>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" }) <!--Checks for valid form submission-->

                <div class="form-group">
                    @Html.LabelFor(model => model.name, "Pet Name", htmlAttributes: new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        <!--Dropdown items-->
                        @Html.EditorFor(model => model.name, new { htmlAttributes = new { @class = "form-control" } })
                    </div>
                </div>
                @*
                    <button data-toggle="collapse" data-target="#hiddenDiv1" class="btn btn-default">Add/Remove a Pet</button>
                    <br />

                    <div id="hiddenDiv1" class="collapse">
                        <div class="form-group">
                            <div class="col-md-10">
                                <!--Dropdown items-->
                                <select name="pet" id="pet2">
                                    @foreach (var item in Model)
                                    {
                                        <option>@Html.DisplayFor(modelItem => item.name)</option>
                                    }
                                </select>
                            </div>
                        </div>
                        <button data-toggle="collapse" data-target="#hiddenDiv2" class="btn btn-default">Add/Remove a Pet</button>
                        <br />
                    </div>

                    <div id="hiddenDiv2" class="collapse">
                        <div class="form-group">
                            <div class="col-md-10">
                                <!--Dropdown items-->
                                <select name="pet" id="pet3">
                                    @foreach (var item in Model)
                                    {
                                        <option>@Html.DisplayFor(modelItem => item.name)</option>
                                    }
                                </select>
                            </div>
                        </div>
                        <button data-toggle="collapse" data-target="#hiddenDiv3" class="btn btn-default">Add/Remove a Pet</button>
                        <br />
                    </div>

                    <div id="hiddenDiv3" class="collapse">
                        <div class="form-group">
                            <div class="col-md-10">
                                <!--Dropdown items-->
                                <select name="pet" id="pet4">
                                    @foreach (var item in Model)
                                    {
                                        <option>@Html.DisplayFor(modelItem => item.name)</option>
                                    }
                                </select>
                            </div>
                        </div>
                        <button data-toggle="collapse" data-target="#hiddenDiv4" class="btn btn-default">Add/Remove a Pet</button>
                        <br />
                    </div>

                    <div id="hiddenDiv4" class="collapse">
                        <div class="form-group">
                            <div class="col-md-10">
                                <!--Dropdown items-->
                                <select name="pet" id="pet5">
                                    @foreach (var item in Model)
                                    {
                                        <option>@Html.DisplayFor(modelItem => item.name)</option>
                                    }
                                </select>
                            </div>
                        </div>
                *@
            </div>

            <!--Submit button-->
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Select Pet" class="btn btn-block btn-primary" />
                </div>
            </div>
        }
       
        <br />
        <!--Link to register a new pet-->
        <div class="form-horizontal">
            <h4>New Pet</h4>
            <hr />
            <p>If you have not yet registered a pet with us, or if you want to register a new pet, click below.</p>
            <p>
                @Html.ActionLink("Register New Pet", "Register_Pet", "Booking", new { area = "" }, new { @class = " btn btn-primary" }) <!--Button to Register_Pet page-->
            </p>
        </div>
    </div>
</div>