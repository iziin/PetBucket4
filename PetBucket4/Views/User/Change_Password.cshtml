
@model PetBucket4.Models.Customer

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml"; <!--defines page layout-->
}

<!--Page for users to change their account password-->
<!--This page does not currently function with the DB-->
<!--Page not linked to success page-->

<!--New row-->
<div class="row">
    <!--Main insert-->
    <div class="col-lg-7 large-insert">
        <h1>Change Password</h1>
        <br />
        
        <!--Not currently linked to db-->
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <h2 class="sub-heading">Customer</h2>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <div class="form-group">
                    @Html.LabelFor(model => model.password, "Current Password", htmlAttributes: new { @class = "control-label col-md-2" }) <!--Label for Password field-->
                    <div class="col-md-10">
                        @Html.PasswordFor(model => model.password, new { htmlAttributes = new { @class = "form-control", placeholder = "Password" } }) <!--Password field-->
                        @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" }) <!--Checks validation of entry-->
                    </div>
                </div>

                <div class="form-group">
                    @Html.LabelFor(model => model.password, "New Password", htmlAttributes: new { @class = "control-label col-md-2" }) <!--Label for Password field-->
                    <div class="col-md-10">
                        @Html.PasswordFor(model => model.password, new { htmlAttributes = new { @class = "form-control", placeholder = "Password" } }) <!--Password field-->
                        @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" }) <!--Checks validation of entry-->
                    </div>
                </div>

                <!--Submit button-->
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save Changes" class="btn btn-block btn-primary" />
                    </div>
                </div>
            </div>
        }
       
    </div>
</div>