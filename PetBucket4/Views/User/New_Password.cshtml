@model PetBucket4.Models.Customer

@{
    ViewBag.Title = "New_Password";
    Layout = "~/Views/Shared/_Layout.cshtml"; <!--defines page layout-->
}

<!--Page for users to change their account password-->
<!--New row-->
<div class="row">
    <!--Main insert-->
    <div class="col-lg-7 large-insert">
        <h1>Change Password</h1>
        <br />

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-horizontal">
                <h2 class="sub-heading">Enter Your New Password</h2>
                <hr />
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                <!--New Password-->
                <div class="form-group">
                    @Html.LabelFor(model => model.password, "New Password", htmlAttributes: new { @class = "control-label col-md-2" }) <!--Label for Password field-->
                    <div class="col-md-10">
                        @Html.PasswordFor(model => model.password, new { htmlAttributes = new { @class = "form-control", placeholder = "New Password" } }) <!--Password field-->
                        @Html.ValidationMessageFor(model => model.password, "", new { @class = "text-danger" }) <!--Checks validation of entry-->
                    </div>
                </div>

                <!--Submit button-->
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Save Password" class="btn btn-block btn-primary" />
                    </div>
                </div>
            </div>
        }

    </div>
</div>